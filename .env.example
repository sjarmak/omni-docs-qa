# Copy to .env (mode 0600). Never commit real values.
# Name of a logged-in Omni CLI profile. It is a label, not a credential.
OMNI_PROFILE=
# Administrator DSN used only by `python -m omni_docs_qa.load`. Never give it to Omni.
DOCS_QA_ADMIN_DSN=
